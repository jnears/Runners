<%= form_for(@intranet) do |f| %>
  <% if @intranet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@intranet.errors.count, "error") %> prohibited this intranet from being saved:</h2>

      <ul>
      <% @intranet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_name %><br>
    <%= f.text_area :category_name %>
  </div>
  <div class="field">
    <%= f.label :show %><br>
    <%= f.check_box :show %>
  </div>

   <div class="field">
    <%= f.label :start_time_text %><br>
    <%= f.text_field :start_time_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
